/**
 * Phase 5: Demo interface.
 *
 * Will expose:
 *   - Pending proposals view
 *   - Escalation queue with human approve/reject
 *   - Mandate configuration panel
 *   - Audit log viewer with hash-chain verification status
 *
 * The money shot: agent proposes to a non-whitelisted address →
 * policy engine stops it (safety visibly working) → then a clean
 * valid payment flows end-to-end.
 */

export {};
